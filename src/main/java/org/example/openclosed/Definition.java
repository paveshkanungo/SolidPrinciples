package org.example.openclosed;

public class Definition {
    void openclosedDefinition(){
        // Open/Closed Principle(OCP) means “A class should be open for extension but closed for modification”.
        // This means that new functionality can be added through extension (inheritance, composition, interfaces)
        // rather than by modifying existing code. As existing code is already tested and deployed in production,
        // adding modifications would introduce an additional risk and require additional testing effort.
    }
}
